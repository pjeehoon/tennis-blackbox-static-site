
# 정적 사이트 로컬 디버깅 및 리소스 경로 안내

- 이 프로젝트는 정적 HTML 파일로 구성되어 있으므로, 로컬에서 파일을 직접 열면 일부 CSS, JS, 이미지 등 리소스가 상대경로/절대경로 문제로 인해 정상적으로 표시되지 않을 수 있습니다.
- 반드시 정적 서버(예: `python -m http.server`, `npx serve` 등)를 사용해 로컬에서 확인하세요.
- Cloudflare 등 CDN 환경에서는 경로가 정상적으로 처리되어 tennisblackbox.net에서 올바르게 보입니다.
- 워드프레스 플러그인에서 변환된 정적 파일이므로, 동적 기능(댓글, 로그인 등)은 동작하지 않습니다.

- Cloudflare 등 CDN 환경에서는 경로가 정상적으로 처리되어 tennisblackbox.net에서 올바르게 보입니다.
- 워드프레스 플러그인에서 변환된 정적 파일이므로, 동적 기능(댓글, 로그인 등)은 동작하지 않습니다.
